{
  "name": "dynamodb-conformance",
  "version": "3.4.0",
  "description": "Independent conformance test suite for DynamoDB-compatible endpoints",
  "keywords": [
    "dynamodb",
    "conformance",
    "aws",
    "testing",
    "vitest"
  ],
  "homepage": "https://paritysuite.org",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paritysuite/dynamodb-conformance.git"
  },
  "bugs": {
    "url": "https://github.com/paritysuite/dynamodb-conformance/issues"
  },
  "license": "Apache-2.0",
  "author": "Martin Hicks <hello@martinhicks.dev>",
  "private": true,
  "type": "module",
  "workspaces": [
    "site"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:quick": "vitest run --exclude 'tests/tier2/updateTable/gsi.test.ts' --exclude 'tests/tier2/vectorSearch/updateLifecycle.test.ts'",
    "test:gating": "vitest run --exclude 'tests/tier2/updateTable/gsi.test.ts' --exclude 'tests/tier2/vectorSearch/updateLifecycle.test.ts' --exclude 'tests/tier2/export/exportImport.test.ts' --exclude 'tests/tier2/kinesis/streamingDestination.test.ts'",
    "test:integrations": "vitest run tests/tier2/export/exportImport.test.ts tests/tier2/kinesis/streamingDestination.test.ts",
    "test:gsi": "vitest run tests/tier2/updateTable/gsi.test.ts tests/tier2/vectorSearch/updateLifecycle.test.ts",
    "test:watch": "vitest",
    "test:tier1": "vitest run --include 'tests/tier1/**/*.test.ts'",
    "test:tier2": "vitest run --include 'tests/tier2/**/*.test.ts'",
    "test:tier3": "vitest run --include 'tests/tier3/**/*.test.ts'",
    "test:tooling": "vitest run --config vitest.tooling.config.ts",
    "test:capture-ground-truth": "DYNAMODB_ENDPOINT= vitest run --reporter=json --outputFile=ground-truth/latest.json",
    "test:capture-ddb-local": "DYNAMODB_ENDPOINT=http://localhost:8000 vitest run --reporter=json --outputFile=results/dynamodb-local.json",
    "results:table": "node scripts/summarise.mjs",
    "results:tags": "node scripts/tag-manifest.mjs",
    "results:badges": "node scripts/badges.mjs",
    "results:check-leaks": "node scripts/check-account-ids.mjs",
    "test:ui": "vitest --ui",
    "site:dev": "npm run dev --workspace site",
    "site:build": "npm run build --workspace site",
    "site:test": "npm test --workspace site",
    "site:check-build": "npm run check:build --workspace site",
    "site:snapshot": "npm run snapshot --workspace site",
    "results:lineage": "node scripts/lineage.mjs",
    "suite:manifest": "node scripts/suite-manifest.mjs",
    "suite:manifest:check": "node scripts/suite-manifest.mjs --check"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1121.0",
    "@aws-sdk/client-dynamodb-streams": "^3.1121.0",
    "@aws-sdk/lib-dynamodb": "^3.1121.0"
  },
  "devDependencies": {
    "@aws-sdk/client-application-auto-scaling": "^3.1121.0",
    "@aws-sdk/client-kinesis": "^3.1121.0",
    "@aws-sdk/client-s3": "^3.1121.0",
    "@types/node": "^26.4.0",
    "js-yaml": "^5.4.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
